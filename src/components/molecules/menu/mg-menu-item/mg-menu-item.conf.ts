/**
 * List of all possibles sizes
 */
export const sizes: string[] = ['regular', 'large'];

/**
 * Available menu item status
 */
export enum Status {
  VISIBLE = 'visible',
  HIDDEN = 'hidden',
  DISABLED = 'disabled',
  ACTIVE = 'active',
}
